export default function MentorQuizManagementPage() {
  return (
    <div className="space-y-4">
      <section className="card">
        <h1 className="text-xl font-semibold">Quiz Management</h1>
        <p className="mt-2 text-sm text-slate-300">
          Mentor workflow is prepared. Current backend exposes quiz retrieval and submission endpoints; quiz creation
          endpoints should be added for full authoring support.
        </p>
      </section>
    </div>
  );
}
